flask==3.1.0
flask-socketio==5.4.1
groq==0.11.0
# groq==0.11.0 still passes `proxies=` into its internal httpx.Client, which
# httpx removed in 0.28 — an unpinned install pulls latest httpx and breaks
# with "Client.__init__() got an unexpected keyword argument 'proxies'".
# Confirmed working pin, hit this exact break during a fresh local install.
httpx==0.27.2
python-dotenv==1.0.1
gunicorn==23.0.0
eventlet==0.39.1
